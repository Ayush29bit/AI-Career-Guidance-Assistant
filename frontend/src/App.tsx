/**
 * One screen, so the app is one line.
 *
 * No router: there is a single destination, and adding one now would be
 * scaffolding for a navigation structure the product does not have.
 */
import { CounsellorPage } from './pages/CounsellorPage'

export default function App() {
  return <CounsellorPage />
}
